# 마이택시플랜 (MyTaxiPlan) — 개인택시 준비자금 계산기

모바일 우선 단일 HTML 웹앱. GitHub Pages로 배포되며 **이미 서비스 중**이다.

- **라이브 주소**: https://calculator.licen.co.kr (루트 `index.html`, `main` 브랜치)
- **브랜드**: MyTaxiPlan / 마이택시플랜
- **운영**: 개인택시 양수도센터 (KOLA 로고) / 한국모빌리티연구소 (KOMO, 푸터)
- **기능**: 면허·차량·보험/조합·차량작업비를 선택하면 준비자금이 자동 계산되고,
  두 가지 구성을 나란히 비교. 견적을 문자로 받아볼 수 있음.

---

## 1. 반드시 지킬 작업 규칙

### 파일 3개를 항상 동기화
계산기 본체는 **같은 내용의 파일이 3개** 있다. 하나만 고치면 안 된다.

| 파일 | 역할 |
|---|---|
| `코드 작업/taxi_calculator_v2.0_navy.html` | **소스 (여기서 먼저 수정)** |
| `index.html` | 라이브 배포본 |
| `preview/index.html` | 검토용 (`/preview` 경로) |

```bash
# 소스 수정 후 반드시
cp "코드 작업/taxi_calculator_v2.0_navy.html" index.html
cp "코드 작업/taxi_calculator_v2.0_navy.html" preview/index.html
diff -q index.html preview/index.html   # 동일 확인
```

### 배포
- **`main` 브랜치 = 라이브.** 푸시하면 1~2분 뒤 실제 서비스에 반영된다.
- 작업 브랜치 `claude/repo-setup-existing-files-2n4ao5` 와 `main` **양쪽에 푸시**해 왔다.
- 라이브에 바로 반영하기 곤란한 변경은 `preview/`에만 올려 검토받는 방식을 써왔다.

### 검증
커밋 전에 최소한 이 두 가지는 확인한다.
1. **JS 문법**: 마지막 `<script>` 블록을 추출해 `node --check`
2. **시각 확인**: Playwright로 모바일(390×844)·PC(1280×800) 스크린샷

```
Playwright: /opt/node22/lib/node_modules/playwright
Chromium  : executablePath '/opt/pw-browsers/chromium'
```

---

## 2. 저장소 구조

```
index.html                     라이브 계산기
preview/index.html             검토용 사본
CNAME                          calculator.licen.co.kr
og-image.png                   링크 미리보기 썸네일 (1200×630)

home/ cafe/ cafe_2/ kakao_open_1/
youtube/ youtube_evans/ sms/ qr/     경로별 UTM 리다이렉트 (8개)

코드 작업/
  taxi_calculator_v2.0_navy.html     ★ 소스
  GAS_전체스크립트.gs                 백엔드 (doGet/doPost, 솔라피 문자)
  홈페이지_시세_자동수집.gs            홈페이지 시세 → 시트 자동 반영
  GA_스프레드시트_연동.gs             GA4 → 시트 일일 집계
  GA4_커스텀_채널그룹_설정.md          ★ GA4 채널 분류 규칙
  시안/ 레퍼런스/                     구버전·참고자료

promo/        로고·QR·배너 등 홍보 자산
쇼츠 작업/     쇼츠 컷 프롬프트 및 컷 영상
디자인 작업/   디자인 시안
```

---

## 3. 연동 시스템

### Google Apps Script (백엔드)
- 스프레드시트 `자금계산기 DB` (ID `16tTQilanjKsumRLmSHrbeBghgdMNF2p9FjemZq-9Qco`) 를
  `openById` 로 열어 사용한다. 컨테이너 바인딩 방식이 아니다.
- `doGet` — 시세·차량·보험·대출 데이터를 계산기에 내려준다
- `doPost` — 문자 신청을 `자금계산기 응답DB` 에 기록하고 솔라피로 발송

### GA4
- 측정 ID `G-GHT8R1LSBK`
- 경로별 UTM으로 유입을 구분한다. 자세한 규칙과 이유는
  `코드 작업/GA4_커스텀_채널그룹_설정.md` 참고.

| 경로 | utm_source | utm_medium |
|---|---|---|
| `/home` | homepage | referral |
| `/cafe` | naver_cafe_official | cafe_official |
| `/cafe_2` | naver_cafe_taxiunion | cafe_union |
| `/kakao_open_1` | kakao_openchat | kakao_open_1 |
| `/youtube` | youtube | youtube |
| `/youtube_evans` | youtube_evans | youtube_evans |
| `/sms` | sms | sms |
| `/qr` | qr | qr |

---

## 4. 이미 밟은 지뢰 (다시 밟지 말 것)

**CSS·레이아웃**
- `html,body{overflow-x:hidden}` 은 세로축을 `auto`로 만들어 **PC 스크롤을 망가뜨린다.**
  → `overflow-x:clip` 사용.
- 고정 오버레이(커버·비교표·모달)를 띄울 때 문서 스크롤을 잠그지 않으면
  **스크롤바가 두 줄**로 보인다. → `syncScrollLock()` 이 이를 처리한다. 우회하지 말 것.
- 로고 세로 정렬은 눈대중으로 맞추지 말 것. 렌더링 픽셀의 **잉크 중심을 측정**해서 맞춰왔다.
  커버 배경의 글로우·링 장식이 측정을 오염시키므로, 측정할 때는 잠시 숨긴다.

**스프레드시트·GAS**
- `GAS_전체스크립트.gs` 는 **열 주소를 하드코딩**해서 읽는다(D~P, W, Y~AA).
  **중간에 열을 삽입하면 뒤쪽 데이터를 전부 잘못 읽는다.** 새 데이터는 별도 시트로 분리할 것.
- `M열(OBD 단자 설치 비용)` 은 시트에 있지만 **코드가 읽지 않는다.** (미해결)
- 응답DB **H열(GA 클라이언트 ID)** 이 숫자로 저장돼 뒷자리가 잘린다.
  (`622710381.1771234567` → `622710381.2`) 텍스트 서식으로 바꿔야 한다. (미해결)

**GA4**
- 채널 규칙 조건은 반드시 **"정확히 일치"**. `포함`을 쓰면 `naver_cafe` 가
  대표카페·택시연합을 모두 흡수해 분리가 깨진다.
- 링크를 나누기 전 두 카페에 같은 링크를 올렸던 기간이 있어, 그 시기 데이터는
  카페 구분이 **원천적으로 불가능**하다. `medium=community` 로 격리해 읽는다.

---

## 5. 진행 중 / 대기 중인 일

| 작업 | 상태 | 다음 행동 |
|---|---|---|
| **GA4 채널 그룹** | 규칙 확정, 문서화 완료 | GA4 콘솔에 표대로 입력 (사람이 해야 함) |
| **홈페이지 시세 자동수집** | 코드 완성·검증 완료 | Apps Script에 붙여넣고 `시세_미리보기` → `시세_자동갱신` → `트리거_설정` |
| **홍보 쇼츠** | 컷1~4 프롬프트 확정, 컷 영상 4개 확보 | 앱 화면녹화(실데이터) + CapCut 편집 |
| **차량 옵션 표** | 설계안 제시, 결정 대기 | 옵션 선택 방식 결정 (개별선택 / 표시만 / 추천세트) |
| **응답DB H열** | 원인 파악 완료 | 텍스트 서식으로 고칠지 결정 |

---

## 6. 쇼츠 작업 요약

두 사람(민수·지훈)이 카페에서 대화하다 계산기를 소개하는 20초 내외 세로 영상.
Google Flow(Veo)로 생성한다.

```
컷1 (민수 고민) → 컷2 (지훈이 폰을 밀어줌, 카메라가 화면으로 다이빙)
→ [앱 화면녹화] → 컷3 (민수 반응) → 컷4 (지훈 마무리)
```

- 프롬프트 전문: `쇼츠 작업/쇼츠_컷_프롬프트.md`
- 생성된 컷 영상: `쇼츠 작업/컷 영상/*.mp4`

**Veo 프롬프트 요령** (시행착오로 얻은 것)
- 부정문(`No text` 등)을 쓰면 오히려 그 요소를 그린다. **긍정 묘사로 우회**할 것.
- 브랜드명·URL을 넣으면 **엉뚱한 화면을 지어낸다.** 넣지 말 것.
- 컷당 **말하는 사람은 한 명만** 명시해야 입 모양이 분산되지 않는다.
- 인물 일관성은 프롬프트로 안 잡힌다. Flow의 **캐릭터 자산**을 첨부해야 한다.
- 손으로 물건을 **돌리는 동작은 거의 항상 깨진다.** 밀기 등 단순 동작으로 대체.
- 대사를 비워두면 모델이 **한국어 대사를 지어낸다.**
- 따옴표로 대사를 넣으면 **자막이 화면에 구워진다.** 프롬프트로는 막을 수 없어,
  편집에서 자막 바로 덮는 방식으로 처리하기로 했다.

---

## 7. 참고 문서

| 문서 | 내용 |
|---|---|
| `코드 작업/GA4_커스텀_채널그룹_설정.md` | GA4 채널 분류 규칙, 과거 데이터 해석 지침 |
| `코드 작업/홈페이지_시세_자동수집.gs` | 시세 자동 반영 (사용법이 파일 상단 주석에 있음) |
| `코드 작업/GAS_문자발송_솔라피.md` | 문자 발송 연동 |
| `코드 작업/디자인_핸드오프.md` | 디자인 규격 |
| `쇼츠 작업/쇼츠_컷_프롬프트.md` | 쇼츠 컷별 프롬프트 |
