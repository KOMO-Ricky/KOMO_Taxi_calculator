# Claude / Codex 공통 작업 기록

두 도구의 대화는 자동 공유되지 않는다. 작업 시작 시 이 문서와 CLAUDE.md / AGENTS.md를 함께 읽고, 작업 종료 시 변경 사항·검증·배포 상태·남은 일을 이 문서에 갱신한다. 로컬 수정, GitHub 배포, Apps Script 배포를 구분해서 기록한다.

## ~2026-09-21 이전 작업 기록 (Claude — 상세 규칙·경위는 CLAUDE.md)

- **계산기 v2.0 Navy 라이브 운영** — calculator.licen.co.kr, 소스·index·preview 3파일 동기화 규칙.
- **GAS 백엔드** — doGet(시세·차량·보험·대출) / doPost(솔라피 문자·의견 접수). Apps Script 프로젝트 "자금계산기/자격진단".
- **GA4** — 경로별 UTM 8종 + 커스텀 채널그룹 규칙 문서화 (`코드 작업/GA4_커스텀_채널그룹_설정.md`). 콘솔 입력은 대기.
- **홈페이지 시세 자동수집** — `홈페이지_시세_자동수집.gs` 작성, Apps Script 프로젝트에 붙여넣기 완료 (트리거 설정 여부 미확인).
- **홍보 쇼츠** — AI 컷(50대 버전) + 시연 인서트 + 아웃트로 풀버전 v2/v3 완성. 재생성 파이프라인 `쇼츠 작업/시연영상_제작/README.md`.
- **차량별 할부율·옵션 시트 실험** (2026-09-21) —
  - 원본 `자금계산기 DB` 탭 전 영역(A1:AN30) 수식 검사: **수식 셀 없음** (getFormulas, `할부율_옵션_사본구성.gs`의 `수식검사()`).
  - '자금계산기 DB의 사본' 탭에 할부율 열 실험 구성 (이후 사용자·Codex의 원본 개편으로 역할 종료 — 삭제 가능).
  - **'차량옵션' 탭 신설**: 차량명 세로 병합 | 옵션명 | 옵션금액(기입 대기) | 추천세트(V) | 비고(기준 트림). 원자료는 `차량 할부율 표.xlsx`의 추천옵션 내용 분해.

## 2026-09-21 — 시트 연동 및 차량 할부수수료율

### 작업 방식과 범위
- 사용자가 Google Sheets의 열·행을 직접 옮기고 데이터를 입력한다. 에이전트는 전달받은 배치에 맞춰 저장소 코드를 수정한다. 시트를 직접 조작하지 않는다.
- 대상: 스프레드시트 ID `16tTQilanjKsumRLmSHrbeBghgdMNF2p9FjemZq-9Qco`, 원본 탭 `자금계산기 DB`.
- 원본 탭 전체의 수식 존재 여부는 직접 검사하지 못했다. 수식이 없다고 단정하면 안 된다.

### 확정된 현재 셀 배치
차량 데이터는 기존과 같이 3행부터 읽는다. D열이 빈 행은 건너뛴다.

| 데이터 | 현재 위치 | 변경 |
|---|---|---|
| 차량 기본 정보 | D~M | 유지. M열 OBD 비용 미연동은 기존 미해결 사항 |
| 기본 연 할부수수료율, 24/36/48/60개월 | N/O/P/Q | 신규 |
| 프로모션 연 할부수수료율, 24/36/48/60개월 | R/S/T/U | 신규 |
| 프로모션 적용 조건 | V | 신규 |
| 차량별 공제·보험료 최소/최대 | X/Y/Z | 기존 N/O/P에서 이동 |
| 기존 R~AI 블록 | AB~AS | 10열 오른쪽 이동 |
| 필수 작업비 | AC3:AC6 | 기존 S3:S6 |
| 선택 작업비 | AG3:AG9 | 기존 W3:W9 |
| 은행명/한도/이율 | AI/AJ/AK, 3행부터 | 기존 Y/Z/AA |
| 추천 구성 | AM4:AS4 | 기존 AC4:AI4 |
| 면허 시세 기준일 | B9 | dataDate와 dateLicense로 전달 |
| 차량 가격 기준일 | B15 | 신규 dateCar |
| 대출 기준일 | B18 | 기존 B15 |
| 조합 가입비 | B21 | 기존 B18 |

### 구현된 동작
- GAS doGet에 carBaseRates / carPromoRates / carPromoConditions 추가. 퍼센트 셀의 소수값과 퍼센트 문자열을 연 % 숫자로 변환하고 빈칸과 실제 0%를 구분한다.
- 웹페이지에서 차량별 기본/프로모션 수수료율을 선택하며 프로모션 조건을 표시한다. 조건의 실제 충족 여부를 자동 심사하는 기능은 없다.
- 개월수 버튼은 24/36/48/60개월. 용어를 '할부수수료율'로 통일하고 수수료율 종류 드롭다운에 기존 차량 할부 여부와 동일한 ▼ 화살표를 넣었다.
- 선택한 수수료율이 비어 있으면 예시값으로 월 납입금을 계산한다. 입력된 실제 0%는 유지한다.

| 예시값 | 24개월 | 36개월 | 48개월 | 60개월 |
|---|---:|---:|---:|---:|
| LPG 및 기타 | 6.0% | 6.2% | 6.4% | 6.6% |
| 전기 | 5.0% | 5.2% | 5.4% | 5.6% |

- 전기는 기존 isEV 판별(DB.carFuels 값이 '전기')을 사용한다.
- N~V에 정보가 전혀 없으면 수수료율 종류 선택을 숨기고 전체 미제공 안내를 표시한다. 조건(V)만 있어도 일부 데이터가 있는 차량으로 분류한다.
- 전체 미제공 안내: “※ 제조사로부터 할부수수료율 데이터가 제공되지 않는 차량입니다. **유사 차종 및 개월수를 기준으로 한 평균값으로 적용됩니다.**”
- 선택 기간 미제공 안내: “※ 제조사로부터 해당 개월 수에 대한 할부수수료율 데이터가 제공되지 않습니다. **유사 차종 및 개월수를 기준으로 한 평균값으로 적용됩니다.**”
- 예시값 적용 시 월 납입금 아래: “실제 이율은 **개인 신용·선납금·개월수·캐피탈 심사**와 그 달의 **프로모션(무이자·저금리 포함)**에 따라 달라질 수 있습니다.”
- 시트 값 적용 시: “제조사로부터 제공되는 기본적인 할부수수료율 정보를 기반으로 계산됩니다. 구매월의 프로모션 및 개인 신용·선납금·개월수·캐피탈 심사 등에 따라 달라질 수 있습니다.”
- 위 '평균값' 문구와 예시 수수료율은 사용자가 지정했다. 실제 시장 평균을 조사한 값은 아니다.
- 차량 가격 기준일 박스를 취등록세/공채 아래에서 차량 소계 바로 아래로 이동했다.
- 면허 기준일 누락 원인: GAS는 B9를 dataDate로 보내고 화면은 dateLicense를 읽었다. GAS에 dateLicense를 추가하고 화면에도 d.dateLicense || d.dataDate 호환 처리를 추가했다.

### 파일 및 검증
- 수정: `코드 작업/GAS_전체스크립트.gs`, `코드 작업/taxi_calculator_v2.0_navy.html`, `index.html`, `preview/index.html`.
- HTML 3개는 동일 내용으로 동기화했다.
- JS 문법, 이동 셀 매핑 모의 응답, 퍼센트/빈칸/0% 구분, 연료·기간별 예시값, 프로모션, 월 납입금, B9 응답 및 이전 응답 호환 테스트 통과. git diff --check 통과.
- 브라우저 스크린샷 시도는 커버 화면만 촬영되어 변경된 차량 모달의 시각 검증을 완료한 것으로 보지 않는다.

### 배포 상태 (2026-09-21 확인)
- 사용자 커밋/푸시: `0a788e0757f0e3e1ea069fa0c3026a9234244cc3` (chatgpt 수정내용(할부/연동)), main.
- GitHub Pages 배포 성공: https://github.com/KOMO-Ricky/KOMO_Taxi_calculator/actions/runs/35575003340
- 공개 사이트 응답에서 EXAMPLE_INSTALLMENT_RATES, 면허 기준일 호환 코드, 24/36/48/60개월 버튼 정의를 확인했다. 당시 Last-Modified는 2026-09-21 07:53:12 UTC.
- 이전 화면이 남아 보이면 강력 새로고침 또는 버전 쿼리 URL로 확인한다. 배포 실패로 단정하지 않는다.
- 사용자가 차량별 할부율 도입 후 Apps Script 배포 완료를 알렸다. 이후 B15/B18/B21 및 dateLicense 변경에 대한 GAS 재배포 여부는 별도로 확인되지 않았다.
- GitHub push는 Apps Script를 갱신하지 않는다. 필요한 경우 수정된 doGet만 반영하고 기존 배포를 새 버전으로 갱신한다. 실제 운영의 문자 API 키/설정은 보존한다.

### 남은 확인 / 별도 작업
- 실제 시트와 현재 GAS 응답에서 dateCar(B15), dateLoan(B18), combineFee(B21) 반영 여부 확인.
- 차량 모달 모바일/PC 시각 검증 및 실제 데이터로 사용자 흐름 확인.
- 차량 상세 옵션을 별도 탭으로 분리하고 차량명 병합, 추천 세트 V 표시, 합계검증 제거하자는 이전 요청은 이 작업에서 구현하지 않았다.
- 쇼츠 작업 관련 이전 대화는 긴 기록이 있으나 이번 세션에서는 파일·완료 상태를 재검증하지 않았다. 기존 영상 문서와 실제 산출물을 먼저 확인할 것.

## 2026-09-21 — 차량옵션 탭 → 계산기 개별 선택 연동 (Claude)

### 구현
- **GAS doGet에 `carOptions` 추가** (`코드 작업/GAS_전체스크립트.gs` + Apps Script `자금계산기.gs` 동일 반영·저장 완료):
  - '차량옵션' 탭을 2행~마지막 행까지 **동적으로** 읽는다. A열 빈칸 = 직전 차량명 계승(병합 셀 대응).
  - 형식: `{ 차량명: [{n:옵션명, p:금액, r:추천세트여부}] }`.
  - **금액이 비었거나 0이면 그 옵션은 응답에서 제외** → 시트에 금액을 채우는 순간 계산기에 자동 등장. 차종·옵션 추가/삭제·금액 수정 모두 **재배포 없이** 시트만으로 반영된다. (GAS **코드**를 바꿀 때만 새 버전 배포 필요)
- **계산기 프런트** (3파일 동기화 완료):
  - 차량 모달: 해당 차량에 옵션 데이터가 있으면 기존 "옵션 금액 [포함]" 토글 대신 **옵션별 체크리스트** 표시 (추천세트는 기본 체크 + "추천" 배지, 개별 금액 표기, 합계 실시간 갱신).
  - 옵션 데이터가 없는 차량(금액 미기입·수입차 등)은 **기존 추천옵션 세트 방식으로 자동 폴백**.
  - 선택 상태는 옵션명 배열(`optSel`)로 저장 → 시트에서 옵션 순서가 바뀌어도 안전. 과거 저장분(optOn)과 호환.
  - 문자 견적: 선택 옵션명 목록 + 합계 표기. 비교표·총액은 기존 계산 경로(carFullOf/carInstOf) 그대로 관통.

### 검증
- JS 문법(node --check) 통과. Playwright 스텁 테스트: 체크리스트 노출·추천 기본 체크·합계(추가/해제) 갱신·저장 후 목록 반영·레거시 폴백(옵션 데이터 없는 차량) 모두 통과.
- 라이브 영향 없음 확인: 현재 '차량옵션' 탭은 금액 미기입 → carOptions가 빈 객체 → 전 차량 기존 동작 유지.

### 배포 상태
- 로컬 커밋: 소스+index+preview+GAS+문서 (아래 커밋 참조). **GitHub push는 인증 대기** (이 환경에서 자격 증명 없음).
- Apps Script: `자금계산기.gs` 수정·저장 완료. **웹앱 새 버전 배포는 미완** — 편집기 배포 대화상자가 자동화 환경에서 반복 멈춤. 사용자: 배포 ▶ 배포 관리 ▶ ✏️ ▶ 버전 '새 버전' ▶ 배포 (1회).

### 남은 일
- 사용자: '차량옵션' 탭 C열 금액 기입 → Apps Script 새 버전 배포 1회 → 이후는 시트 수정만으로 자동 반영.
- '자금계산기 DB의 사본' 탭 삭제 여부 결정 (역할 종료).
- Codex의 남은 확인 항목(dateCar/dateLoan/combineFee 반영, 차량 모달 시각 검증)은 그대로 유효.
